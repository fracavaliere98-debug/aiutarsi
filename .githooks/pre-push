#!/bin/sh
set -eu

ROOT_DIR=$(git rev-parse --show-toplevel)

if [ "${SKIP_LOCAL_SMOKE:-0}" = "1" ]; then
  echo "Skipping local iOS smoke tests because SKIP_LOCAL_SMOKE=1"
  exit 0
fi

echo "Running local iOS smoke tests before push..."
cd "$ROOT_DIR"
npm run smoke:ios
