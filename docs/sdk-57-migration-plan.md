# Migrazione a Expo SDK 57 — studio e piano (21/09/2026)

Stato: PIANO, nessuna modifica al codice applicativo. Sostituisce `docs/sdk-55-upgrade-plan.md` del branch `upgrade/expo-sdk-55` (fermo ad aprile, solo bump dipendenze).

## Decision Log

- **Contesto.** L'app è su SDK 54 (RN 0.81.5, React 19.1). Expo Go sull'App Store supporta solo l'SDK più recente (57); l'SDK 58 è in beta (RN 0.88) e Expo Go passerà al 58 poco dopo la stabile. Il test su iPhone con Expo Go è quindi bloccato.
- **Opzioni.** (A) restare su 54 e testare con development build / simulatore; (B) migrare a 57 e continuare con Expo Go; (C) saltare a 58 beta.
- **Scelta.** Migrare a **57** (B) *e* smettere di dipendere da Expo Go per i test seri (A): l'app usa moduli nativi che Expo Go non copre bene (maps, notifiche push, Sentry, updates). Non passare al 58 finché non è stabile: richiede Node 22+, iOS 27 con SceneDelegate, rework del core di expo-router e R8 di default.
- **Trade-off.** 3 SDK in un colpo (55, 56, 57) = più superficie di rischio; mitigato procedendo per gradini con verifica a ogni gradino. iOS minimo (vedi punto 12): da verificare a ogni gradino, non assumere 16.4 da SDK 56 senza controllo diretto (vedi correzione sotto).

## Requisiti degli store: cosa risulta oggi (VERIFIED su fonti ufficiali)

- **App Store:** dal 28/04/2026 gli upload richiedono Xcode 26 / SDK iOS 26. Expo dichiara SDK 54 e 55 già conformi (immagine EAS di default con Xcode 26). `eas.json` non fissa nessuna immagine → siamo conformi. **Nessuna urgenza store dall'SDK.**
- **Google Play:** dal 31/08/2026 nuovi app/aggiornamenti devono avere target API 36 (estensione richiedibile fino al 01/11/2026); le app esistenti devono targettare almeno API 35 per restare visibili. RN 0.81 usa `targetSdk = 36`, `compileSdk = 36`, `minSdk = 24` → conformi.
- Da SDK 56: Xcode ≥ 26.4. iOS minimo: **CORREZIONE (gradino 55→56, verificato su `node_modules/react-native/scripts/cocoapods/helpers.rb`)** — a RN 0.85.3 `min_ios_version_supported` è ancora **15.1**, non 16.4. L'assunzione iniziale era sbagliata (probabile confusione con un requisito Xcode/App Store, non con il deployment target dell'app). Da riverificare al gradino 56→57 (RN 0.86). Da RN 0.85: Node ≥ 20.19.4 (CI usa Node 20 → ok).

## Matrice versioni (da `expo/bundledNativeModules.json` di SDK 57.0.24)

expo ~57.0.24 · react/react-dom 19.2.3 · react-native 0.86.3 · expo-router ~57.0.22 · expo-notifications ~57.0.20 · expo-updates ~57.0.23 · expo-location ~57.0.19 · expo-image-picker ~57.0.19 · expo-constants ~57.0.19 · expo-file-system ~57.0.7 · expo-calendar ~57.0.4 · expo-video ~57.0.4 · expo-image ~57.0.5 · expo-splash-screen ~57.0.9 · expo-linking ~57.0.10 · reanimated 4.5.1 · worklets 0.10.1 · gesture-handler ~2.32.0 · screens ~4.26.0 · safe-area-context ~5.7.0 · svg 15.15.4 · maps 1.27.2 · webview 13.16.1 · view-shot 5.1.0 · eslint-config-expo ~57.0.2. Sentry: `@sentry/react-native` è escluso da `expo install --fix`; tenere la 8.x più recente (peer `react-native >= 0.65`).

## Rischi specifici del nostro codice

1. **OTA / runtimeVersion (alto).** `runtimeVersion.policy = "appVersion"` con versione `1.0.0`: un OTA con codice SDK 57 pubblicato su un binario SDK 54 con la stessa versione lo manda in crash. Il workflow OTA parte a ogni push su `main`. Regola: nessun codice SDK 57 su `main` finché non esistono build 57; nel branch passare a `policy: "fingerprint"` (o bump di versione).
2. **NativeWind 2.0.11 (alto per la UI).** 88 file usano `className`. La v2 non è più mantenuta; compatibilità con RN 0.86 non verificabile a tavolino. Spike per primo (bundle + render sul simulatore). Piano B: NativeWind 4 (lavoro a parte) o migrazione a StyleSheet dei soli punti che rompono.
3. **`@react-navigation/*` (SECONDA CORREZIONE, questa volta verificata via Expo Go reale — la prima "correzione" al gradino 55→56 era sbagliata).** Testando il branch su Expo Go (SDK 57) l'app si è rifiutata di partire: `Error: As of SDK 56, expo-router is no longer compatible with react-navigation`. La mia analisi statica al gradino 55→56 (npm ls + grep sul codice di expo-router) mi aveva portato alla conclusione sbagliata che `@react-navigation/bottom-tabs` andasse tenuto. Causa vera, trovata leggendo il controllo effettivo in `node_modules/expo/node_modules/@expo/cli/build/src/start/server/metro/withMetroMultiPlatform.js` (righe ~592-609): Metro, quando `expo-router` è installato, blocca a livello di *resolver* qualsiasi import da `@react-navigation/*` fatto da codice applicativo (non da `node_modules`) — non è una questione di quale pacchetto sia installato, è un errore lanciato al momento della risoluzione dell'import stesso in `hooks/useBottomScreenInset.ts`. Fix ufficiale (verificata via https://docs.expo.dev/router/migrate/sdk-55-to-56/ e applicata con il codemod reale, non a mano): `npx expo-codemod sdk-56-expo-router-react-navigation-replace hooks/useBottomScreenInset.ts` → `import { BottomTabBarHeightContext } from "@react-navigation/bottom-tabs"` diventa `import { BottomTabBarHeightContext } from "expo-router/js-tabs"`. Verificato che `expo-router/js-tabs` esporta davvero `BottomTabBarHeightContext` (catena: `js-tabs.js` → `build/layouts/Tabs` → `export * from '../react-navigation/bottom-tabs'`, che contiene `BottomTabBarHeightContext`, `useBottomTabBarHeight`, ecc. — non un'assunzione, letto nei `.d.ts`). `@react-navigation/bottom-tabs` rimosso da `package.json` (era l'unico import `@react-navigation/*` in tutto il codice applicativo, grep su tutto il repo esclusi `node_modules`) e da `node_modules` (6 pacchetti rimossi con `npm install`, incluse le transitive `@react-navigation/native`/`core`/`elements`). tsc/eslint/regressione tornati verdi dopo la rimozione.
4. **Reanimated 4.1→4.5 / worklets 0.5→0.10 (VERIFICATO al gradino 56→57, rischio reale ma non un bug runtime).** Confermato: `babel-preset-expo` 57 inietta automaticamente `react-native-worklets/plugin` quando `react-native-worklets` è installato (`configs/expo.js`, `options.worklets !== false`), e il nostro `babel.config.js` elencava ANCHE esplicitamente `react-native-reanimated/plugin` — che a questa versione è solo un re-export di `react-native-worklets/plugin` (`node_modules/react-native-reanimated/plugin/index.js`). Quindi lo stesso plugin veniva eseguito due volte. Test diretto con `@babel/core` su un file reale (`hooks/useCardAnimation.ts`, usa `scale.value = withSpring(...)`) con e senza il plugin duplicato: nessun errore in entrambi i casi, output byte-per-byte identico (il plugin è idempotente) — quindi non era un bug silenzioso attivo, ma un doppio passaggio ridondante e non allineato alla guida ufficiale. Fix applicato: rimosso `react-native-reanimated/plugin` da `babel.config.js`, tenuto solo `nativewind/babel`; verificato via transform diretto che il worklet marker è comunque presente nell'output.
5. **Hermes V1 di default (SDK 56).** Regressioni di memoria e startup risolte in 57.0.9 / 57.0.17: partire da ≥ 57.0.24 (ok).
6. **expo-file-system (basso).** Usiamo `expo-file-system/legacy` (2 file): esiste ancora in 57. Nessuna azione ora; migrare al nuovo API in un secondo momento.
7. **expo-calendar (CORREZIONE: era "basso", in realtà ALTO — bug reale trovato e risolto al gradino 55→56).** Non è solo un warning: a SDK 56 l'import di default `"expo-calendar"` punta già alla nuova API a oggetti (`getCalendars`, `createCalendar`, classe `ExpoCalendar`, ecc.); i nomi classici usati da `utils/calendar.ts` (`getCalendarsAsync`, `getDefaultCalendarAsync`, `getCalendarPermissionsAsync`, `requestCalendarPermissionsAsync`, `createEventAsync`) esistono ancora nel default export ma sono stub che **lanciano un errore a runtime** (`build/legacyWarnings.js`, verificato riga per riga). Senza fix, "Aggiungi al calendario" si sarebbe rotto silenziosamente al primo build SDK 56+. Fix applicato: i due `await import("expo-calendar")` in `utils/calendar.ts` sono ora `await import("expo-calendar/legacy")`, che espone esattamente le stesse funzioni (verificato via `build/legacy/Calendar.d.ts`) — comportamento identico, nessun'altra modifica al file.
8. **app.json.** Rimuovere `newArchEnabled` e `edgeToEdgeEnabled` (chiavi tolte in 55). `predictiveBackGestureEnabled` da riverificare con `expo-doctor`. Edge-to-edge Android è comunque obbligatorio (target API 36): controllare gli insets su Android.
9. **Nessun impatto atteso** (verificato via grep): `allowsFullscreen` (expo-video), `experimentalBlurMethod`, `addClipboardListener`, `removeSubscription`, `expo-av`, `@expo/vector-icons` non sono usati. Le API di expo-notifications che usiamo esistono ancora in 57 (verificato sui tipi).
10. **Dipendenze non importate da nessun file JS** (riducono superficie): `react-native-volume-manager`, `react-native-view-shot`, `expo-blur`, `expo-symbols`, `clsx`, `tailwind-merge`, `@react-navigation/elements`, `@react-navigation/native`, `@expo/vector-icons`. Da rimuovere prima della migrazione (peer da tenere: gesture-handler, screens, svg, worklets, system-ui, react-native-web/react-dom).
11. **Android:** non c'è un device Android. L'emulatore di Android Studio (gratis) copre notifiche, edge-to-edge e maps.
12. **iOS minimo:** vedi correzione nella sezione "Requisiti degli store" — a RN 0.85.3 (SDK 56) è ancora 15.1, non 16.4. Se il bump a 16.4 arriva con RN 0.86 (SDK 57), resta comunque un impatto sugli iPhone fermi a iOS 15 (6s/7) — il target include utenti anziani/poco digitali: decisione di prodotto da confermare quando confermato a quale gradino avviene davvero.

13. **`StyleSheet.absoluteFillObject` rimosso (ALTO — bug reale trovato e risolto al gradino 55→56).** Tra RN 0.83 e 0.85 l'export `absoluteFillObject` (l'oggetto di stile piatto, spreadabile: `{position:'absolute', left:0, right:0, top:0, bottom:0}`) è sparito sia dai tipi sia dal runtime di RN (resta solo `absoluteFill`, uno stile registrato non spreadabile — sostituirlo 1:1 avrebbe rotto silenziosamente il positioning). 3 usi trovati (`app/_layout.tsx`, `app/onboarding/npo-category.tsx` ×2, `components/GemmaAvatar.tsx`): tutti spread (`...StyleSheet.absoluteFillObject`) dentro altri stili. Fix applicato: le 4 proprietà inlineate direttamente al posto dello spread, comportamento identico, nessuna dipendenza da un export che non esiste più.
14. **`eslint-plugin-react-hooks` v5→v7 via `eslint-config-expo` (medio, solo tooling — trovato e mitigato al gradino 55→56).** Il bump porta di default un nuovo blocco di regole "React Compiler readiness" (`react-hooks/refs`, `set-state-in-effect`, `preserve-manual-memoization`, `purity`, `immutability`, `static-components`): 114 errori lint su file mai toccati dalla migrazione. Verificato che il progetto non usa React Compiler (`app.json` non ha `experiments.reactCompiler`, `babel-preset-expo` lo attiva solo se esplicitamente configurato) → nessun impatto runtime, sono falsi positivi rispetto al nostro setup attuale (es. `react-hooks/immutability` marca come errore `sharedValue.value = ...` di Reanimated, che è l'API corretta e documentata). Le 6 regole nuove sono state disattivate in `eslint.config.js` con commento esplicativo; le regole classiche (`rules-of-hooks`, `exhaustive-deps`) restano attive. Decisione da riconfermare se in futuro si valuta di adottare React Compiler.
15. **EMFILE durante `expo export` in questo ambiente sandbox (nessun impatto sul codice).** `npx expo export --platform ios` fallisce in modo riproducibile con `EMFILE: too many open files` dentro `node_modules/lucide-react-native` (libreria icone con migliaia di file), anche alzando l'ulimit del processo: non è un limite di rlimit ma, con ogni probabilità, del mount FUSE della cartella di sviluppo (`.git/index.lock` ha lo stesso limite noto, vedi istruzioni progetto). Stessa categoria del problema Hermes x86/ARM64 già documentato: non è un segnale di regressione, va verificato con una build reale (Fase 2, sul tuo Mac). Segnale disponibile in questo ambiente: risoluzione moduli Metro completata fino al crash EMFILE (fase successiva a quella dove fallirebbe per un vero errore di import/dipendenza mancante), oltre a tsc/eslint/regressione verdi.

16. **`runtimeVersion.policy` (mitigazione applicata al gradino 56→57).** Passato da `"appVersion"` a `"fingerprint"` in `app.json`, come previsto dal rischio 1 ("nessun codice SDK 57 su main finché non esistono build 57"). Verificato che `@expo/fingerprint` è già presente (dipendenza di `expo@57`, non serve aggiungerla) e che il calcolo del fingerprint funziona in questo ambiente sandbox senza toolchain nativo (144 file sorgente, hash calcolato in pochi secondi) — quindi funzionerà anche nella pipeline CI (`ota-deploy.yml` chiama solo `eas update`, che gestisce la policy in modo trasparente, nessuna modifica al workflow necessaria). Rimane da fare prima del merge su `main`: aggiornare `docs/prod-migration-checklist.md`/`README.md` se citano la policy precedente.
17. **iOS minimo — chiusura definitiva (verificato anche a RN 0.86.3/SDK 57).** `node_modules/react-native/scripts/cocoapods/helpers.rb` riporta ancora `min_ios_version_supported = '15.1'` a RN 0.86.3. L'ipotesi "16.4 da SDK 56" del piano iniziale era quindi sbagliata per l'intera migrazione 54→57, non solo per il gradino 56: **nessun impatto sugli iPhone fermi a iOS 15** con questa migrazione. Punto 12 aggiornato di conseguenza.

## Piano

**Fase 0 — Baseline (su `main`, ~1h).**
- Tag `backup-pre-sdk57`. Verificare se esiste già un binario installato (TestFlight/APK) con runtimeVersion `1.0.0`.
- Rimuovere le dipendenze inutilizzate del punto 10 (commit separato, verifica: tsc + regressione + `npx expo export` + `pod install`).
- Baseline: `npx tsc --noEmit`, `npm run test:regression`, `npx expo-doctor`, `npx expo export --platform ios` e `android`, smoke Maestro (`npm run smoke:ios`, già presente in repo).

**Fase 1 — Branch `upgrade/expo-sdk-57` da `main` (il vecchio `upgrade/expo-sdk-55` è obsoleto).** Un commit per gradino, con gli stessi controlli a ogni gradino (tsc, eslint, regressione, `expo export` iOS+Android, `expo-doctor`):
- 54→55: chiavi app.json, `eas update --environment` (già usato negli script), nessun uso di API rimosse.
- 55→56 (FATTO, commit su `upgrade/expo-sdk-57`): bump versioni da `bundledNativeModules.json` di expo 56.0.22; verificato NON serve alcun codemod router/react-navigation (punto 13 sopra, `@react-navigation/bottom-tabs` va tenuto, non tolto); fix reale `expo-calendar` → `/legacy` (punto 7); fix reale `StyleSheet.absoluteFillObject` rimosso da RN 0.85 (punto 13); iOS minimo verificato ancora 15.1, non 16.4 (correzione); disattivate le nuove regole eslint React-Compiler-readiness non pertinenti (punto 14); `expo/fetch` come default fetch: nessuna traccia negli export di `expo@56` né uso nel nostro codice, ipotesi iniziale non confermata, nessuna azione.
- 56→57 (FATTO, commit su `upgrade/expo-sdk-57`): bump versioni da `bundledNativeModules.json` di expo 57.0.24; `@sentry/react-native` allineato a `^8.27.0` (ultima 8.x, 9.x non ancora rilasciata); rimosso il doppio babel plugin reanimated/worklets (punto 4, verificato con test diretto, non un bug attivo ma andava comunque tolto); `runtimeVersion.policy` passato a `fingerprint` (punto 16); iOS minimo confermato 15.1 anche a RN 0.86.3, chiudendo definitivamente il punto 12/17; nessun altro cambio necessario a `app.json` (config risolve senza warning).
- Nel branch aggiornare anche `.github/workflows` (eas-cli pin) e la doc.

**Fase 2 — Build nativa e test (sul tuo Mac, senza device).**
- `npx expo prebuild --clean` → `npx expo run:ios` (simulatore) e `npx expo run:android` (emulatore).
- Gate automatico: `npm run smoke:ios` (login, feed community, iscrizione attività).
- Matrice manuale minima, per ruolo: registrazione/login/reset password, iscrizione attività, candidatura ente, conferma presenze + XP + notifiche, segnalazione post/utente + inbox admin, chat, mappa/posizione, storie, deep link, push, calendario, foto profilo, tab bar/inset su Android.
- Da fare *prima di tutto il resto*: spike NativeWind (rischio 2).

**Fase 3 — Preview.** Build EAS `preview` dal branch (workflow Android esistente; iOS su device richiede Apple Developer Program), OTA sul canale `preview` isolato dal runtimeVersion, backend staging.

**Fase 4 — Merge.** Solo con Fase 2–3 verdi: merge su `main`, aggiornamento checklist prod, nuove build produzione. Il gate di rilascio store resta quello di `docs/prod-migration-checklist.md`.

**Fase 5 — SDK 58** (dopo la stabile, valutando Node 22 in CI, SceneDelegate iOS 27, rework router, R8): decisione separata.

## Cosa posso fare io / cosa serve da te

- Io: Fasi 0–1 (pruning, codemod, bump, fix di codice, tsc/eslint/regressione, `expo export`, aggiornamento doc) — in locale, senza push.
- Tu: Fase 2 (build simulatore/emulatore, Maestro, prove manuali), decisione iOS 16.4, eventuale Apple Developer Program per test su iPhone reale.
